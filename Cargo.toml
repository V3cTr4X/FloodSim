[package]
name = "floodsim"
version = "1.0.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
tokio = { version = "1", features = ["full"] } # Solo si usas código async
colored = "2"
clap = { version = "4", features = ["derive"] }

